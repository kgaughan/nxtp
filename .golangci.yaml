---
linters:
  disable-all: true
  enable:
    # fast, autofix
    - dupword
    - godot
    - gofumpt
    - goimports
    - whitespace

    # fast, nofix
    - dupl
    - gci
    - ineffassign

    # slow, nofix
    - err113
    - errcheck
    - errname
    - errorlint
    - gosimple
    - govet
    - nonamedreturns
    - revive
    - staticcheck
    - typecheck
    - unconvert
    - unused
    - usetesting
    - wrapcheck

    # Undecided
    - tparallel
    - gosec
